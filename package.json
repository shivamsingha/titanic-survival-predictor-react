{
  "name": "root",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "start": "lerna run start",
    "build": "lerna run build",
    "lint": "lerna run lint",
    "lint:fix": "lerna run lint:fix",
    "prepare": "husky install"
  },
  "workspaces": {
    "packages": [
      "web",
      "server"
    ]
  },
  "devDependencies": {
    "husky": "^6.0.0"
  }
}
